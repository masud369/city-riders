// import { createLocation } from '@remix-run/router/dist/history';
// import React, { useContext } from 'react';
// import { Navigate, Outlet, useLocation } from 'react-router-dom';
// import { PassLocation } from '../../App';
// import DestinationFixed from '../DesstinationFixed/DestinationFixed';

// const LocationProtected = () => {
//     // const [location, setLocation] = useContext(PassLocation);
//     // const locationpass = createLocation();
//     // return location.from && location.to? <Outlet/>:<Navigate to="destinationfixed" replace state={{from:locationpass.pathname}} />
// };

// export default LocationProtected;